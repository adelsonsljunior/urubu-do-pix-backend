name: urubu-do-pix-backend

services:
  api:
    image: quarkus/urubu-do-pix-backend-jvm
    ports:
      - 8080:8080
    environment:
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_URL: ${DB_URL}
